{
  "name": "lumina-ai-shopping-assistant",
  "version": "1.0.0",
  "description": "AI Shopping Assistant backend — Netlify Functions + Supabase + Gemini",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4",
    "stripe": "^15.9.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
